# Use the README file as a reference to customize settings.

User:
  fields:
    default: name, password_digest, mail, old_pass, created_at, updated_at
    form: name, password_digest, mail,old_pass
  relationships: 
  application: Application
