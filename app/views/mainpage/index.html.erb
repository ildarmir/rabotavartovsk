    <div class="row">
    
    <fieldset>
	    <legend>Работнику</legend>
	    <div>
		    <div class="span3">
			    <h4><a href="#">Добавить резюме</a></h4>
			    <%= image_tag("addresume.png")%>
		    </div>
		    <div class="span3">
			    <h4><a href="#">Новые вакансии</a></h4>
			    <ul class="unstyled">
				    <% @vac.each do |f| %>
                              
				   <li>               
	               <%= link_to f.position, { :controller => :vacancies,:action => :show, :id => f.id } %> 
	               <% if session[:group_id] == 2 %>
	               <%= link_to image_tag("edit_profile.jpg", :border => 0),{ :controller => :vacancies, :action => :edit, :id => f.id } %> 
	               <%= link_to image_tag("deleteBut.gif", :border => 0), { :controller => :vacancies, :action => :destroy, :id => f.id }, :method => :delete, :confirm => "Are you sure?" %>
	               <% end %>
                       </li><br> 
                       <% end %>
			    </ul>
		    </div>
		    <div class="span3">
			    <h4><a href="#">Популярные вакансии</a></h4>
			    <ul class="unstyled">
                                <% @vac.each do |f| %>
                              
				   <li>               
	               <%= link_to f.position, { :controller => :vacancies,:action => :show, :id => f.id } %> 
	               <% if session[:group_id] == 2 %>
	               <%= link_to image_tag("edit_profile.jpg", :border => 0),{ :controller => :vacancies, :action => :edit, :id => f.id } %> 
	               <%= link_to image_tag("deleteBut.gif", :border => 0), { :controller => :vacancies, :action => :destroy, :id => f.id }, :method => :delete, :confirm => "Are you sure?" %>
	               <% end %>
                       </li><br>
                       <% end %>
				    <br>
				    
			    </ul>
		    </div>
	    </div>
	    </fieldset>
    </div>

	<div class="row">
    
    <fieldset>
	    <legend>Работодателю</legend>
	    <div>
		    <div class="span3">
			    <h4><a href="#">Добавить вакансию</a></h4>
			    <%= image_tag("addvacansy.png") %>

		    </div>
		    <div class="span3">
			    <h4><a href="#">Добавить курсы</a></h4>
			    <%= image_tag("addcourse.png") %>
		    </div>
		    <div class="span3">
			    <h4><a href="#">Последние резюме</a></h4>
		    <ul class="unstyled">
<% @res.each do |r| %>
              <li>
              <%=  link_to r.position, :id => r.id, :controller => :resumes, :action => :show %>
              <% if session[:group_id] == 2 %>
              <%= link_to image_tag("edit_profile.jpg", :border => 0), :controller => :resumes, :action => :edit, :id => r.id %>
              <%= link_to image_tag("deleteBut.gif", :border => 0), {:controller => :resumes, :action => :destroy, :id => r.id }, :method => :delete, :confirm => "Are you sure?"%>
              </li>
              <% end %>
              <% end %>
			    </ul></div>
	    </div>
	    </fieldset>
    </div>
    
