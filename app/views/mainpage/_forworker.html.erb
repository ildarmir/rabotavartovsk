<div class="row">
  <fieldset>
    <legend>Работнику</legend>
    <ul class="thumbnails">
      <li class="span3">
      <div class="thumbnail text-center">
        <h4><a href="/resumes/new" title="Добавить резюме в базу">Добавить резюме</h4>
          <%= image_tag("addresume.png")%></a>
        <br><br>
        <br><br>
      </div>
      </li>
      <li class="span3">
      <div class="thumbnail text-center">
        <h4><%= link_to "Новые вакансии","/vacancies",title: "Вакансии добавленные недавно" %></h4>
        <ul class="unstyled">
          <% @vac.each do |f| %>

            <li>               
            <%= link_to f.position.slice(0..32), { :controller => :vacancies,:action => :show, :id => f.id },title: "Подробнее о вакансии #{f.position}" %> 
            </li><br> 
          <% end %>
        </ul>
      </div>
      </li>
      <li class="span3">
      <div class="thumbnail text-center">
        <h4><%= link_to "Популярные вакансии","vacancies",title: "Вакансии ппо популярности" %></h4>
        <ul class="unstyled">
          <% @pop.each do |f| %>
            <li>               
            <%= link_to f.position.slice(0..32), { :controller => :vacancies,:action => :show, :id => f.id }, title: "Подробнее о вакансии #{f.position}" %> 
            </li><br>
          <% end %>

        </ul>
      </div>
      </li>
    </ul>
  </fieldset>
</div>
